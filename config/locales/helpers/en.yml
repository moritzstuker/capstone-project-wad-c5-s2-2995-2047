en:
  helpers:
    form:
      back: Back
      errors:
        one: error
        other: errors
      form_contains: "The form contains"
      required_fields: "Required fields"
      search: Search
      submit: Submit
    link_to:
      confirm: "Are you sure?"
      show_all: "Show all"
      reset: "Reset"
