<% if model.errors.any? %>
<div class="message is-danger">
  <div class="message-body content">
    <p><strong><%= pluralize(model.errors.count, "error") %> prohibited this user from being saved:</strong></p>
    <ul>
      <% model.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
</div>
<% end %>
