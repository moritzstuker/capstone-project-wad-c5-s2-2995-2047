<%= link_to link_target, role: 'menuitem', class: "menu-item#{ ' orange-color' if params[:filter] == filter.to_s }" do %>
  <%= dot_label("#{filter.label.capitalize} (#{Project.search(params[:query]).where(category: filter).count})", filter.color) %>
<% end %>
